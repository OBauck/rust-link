[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# replace STM32G071C8Rx with your chip as listed in `probe-rs chip list`
runner = "probe-rs run --speed 1000 --chip STM32C071G8Ux"
# runner = "./flash.sh"

[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "info"
